import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: '1919142-ionic-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
